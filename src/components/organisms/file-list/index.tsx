import { FunctionalComponent, h } from "preact";
import style from "./style.scss";

const FileList: FunctionalComponent = () => {
  return <section class={style.fileList}>File list viewer</section>;
};

export default FileList;
